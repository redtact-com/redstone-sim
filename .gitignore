node_modules
dist
*.tsbuildinfo
.wrangler
.DS_Store
*.local
*.local.*

# 一時スクリプト (エージェントや手元の検証で作るもの)。
# git add -A で公開リポへ混入する事故があったので広めに弾く (#303)
zz-*
*.tmp.mjs
.verify-tmp*

# Playwright E2E (issue #70)
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# 実機キャプチャに食わせる回路ファイル (.litematic/.nbt/.schem)。
# 配布物ではなく手元にしかない。定義ファイルの source はファイル名だけを書き、
# 実体はここに置く (#322。置き場所は MC_CIRCUITS_DIR で差し替え可)
tools/mc-harness/circuits/

# Mojang 由来物 (jar / mappings / デコンパイル産物) は絶対にコミットしない
# (docs/research/03_legal-decompile.md 参照)
tools/decompile/jars/
tools/decompile/work/
tools/decompile/out/
.claude/
